# kortex-mcp

MCP server for the Kortex memory layer. Exposes the same tool registry over both stdio (for local agents like Claude Code, Codex CLI) and HTTP/SSE (for remote/web clients). Built on the official `mcp` Python SDK.

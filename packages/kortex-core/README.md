# kortex-core

Domain models, repositories, services, retrieval, skills, embeddings, llm adapters, storage adapters, and security primitives for the Kortex memory layer. The only package that talks to Postgres directly.

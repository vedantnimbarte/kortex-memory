# kortex-api

FastAPI REST surface for the Kortex memory layer. Auto-generated OpenAPI, RFC 7807 errors, cursor pagination, idempotency keys, ETags. Authenticates via API keys (agents) or JWT (dashboard).

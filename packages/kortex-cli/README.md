# kortex-cli

The `kortex` Typer CLI: full admin + user surface. Auth, search, browse, ingest, export, and server admin (migrations, users, keys). User commands talk to `kortex-api` over HTTPS; admin commands talk to the database via `kortex-core`.
